"""setup.py file for turning on setup.cfg file.

File: setup.py

This module contains the setup configuration for break_projects.
It uses setuptools to handle the package distribution and installation.
"""

from setuptools import setup

setup()
